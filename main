from gtoa.utils import run_experiments

if __name__ == "__main__":
    # Parameters
    N = [10, 20, 40, 80]
    I = [5, 10, 20, 40, 60, 80]

    df = run_experiments(N_values=N, I_values=I,
                         Tmax=5000, n_repeats=10, seed=12345, verbose=False)

